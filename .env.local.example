# Supabase — proyecto Mitico (ref ltusdzhggabmbilrjuju)
NEXT_PUBLIC_SUPABASE_URL=https://ltusdzhggabmbilrjuju.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_anon_key_aca

# Solo server / edge — NUNCA exponer al cliente
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aca

# Secreto para firmar la cookie de sesion del empleado (PIN). Generar: openssl rand -base64 32
EMPLEADO_SESSION_SECRET=cambiar_por_un_secreto_largo_aleatorio

# Secreto para el cron de rotacion de fotos (Vercel Cron lo manda como Bearer).
CRON_SECRET=cambiar_por_un_secreto_aleatorio
